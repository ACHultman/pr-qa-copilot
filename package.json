{
  "name": "pr-qa-copilot",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action: PR summary + Playwright screenshot pack + visual diffs",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('no lint configured')\""
  },
  "dependencies": {
    "@actions/artifact": "^2.1.0",
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "openai": "^6.27.0",
    "pixelmatch": "^5.3.0",
    "playwright": "^1.52.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.5.4"
  }
}
