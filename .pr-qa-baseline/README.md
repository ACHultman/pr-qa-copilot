# Baseline screenshots

If you commit PNG screenshots in this directory (example: `home.png`, `pricing.png`), PR QA Copilot will generate pixel diffs for matching routes.

Mapping:
- route `/` → `home.png`
- route `/pricing` → `pricing.png`

Tip: Start with 3–10 high-signal pages.
